import { describe,expect,it } from 'vitest';import { SetAgentMode,type AgentControlRecord } from '@venho/application';
describe('agent control',()=>{it('requires two-step emergency stop confirmation',async()=>{let state:AgentControlRecord={schemaVersion:'1.0',mode:'RUNNING',changedBy:'owner',changedAt:new Date(0).toISOString(),reason:'test'};const controls={get:async()=>state,set:async(v:AgentControlRecord)=>{state=v;}};const audit={append:async()=>undefined};const uow={transaction:async<T>(work:()=>Promise<T>)=>work()};const result=await new SetAgentMode(controls,audit,uow,{now:()=>new Date('2026-07-13T00:00:00Z')}).execute({mode:'EMERGENCY_STOP',actor:'owner',reason:'drill'});expect(result.ok).toBe(false);expect(state.mode).toBe('RUNNING');});});
