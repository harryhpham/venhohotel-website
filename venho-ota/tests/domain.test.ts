import { describe,expect,it } from 'vitest';import { calculateInventory,contextHash,enforceGuardrails,recommendationExpiry,transitionRecommendation } from '@venho/domain';
describe('P0 domain invariants',()=>{it('does not clamp negative inventory',()=>expect(calculateInventory({physicalSellable:3,activeBookingNights:4,maintenanceBlocks:0,confirmedInternalHolds:0,safetyBuffer:1})).toBe(-2));it('blocks stale execution',()=>expect(()=>transitionRecommendation('STALE','EXECUTING')).toThrow());it('produces deterministic context hashes',()=>expect(contextHash({b:2,a:1})).toBe(contextHash({a:1,b:2})));it('enforces effective net minimum',()=>expect(()=>enforceGuardrails({candidateRate:500000,floor:400000,ceiling:800000,commission:100000,promoStacking:50000,paymentCost:10000,minAcceptableNetRate:400000})).toThrow());it('uses 2h TTL for near dates',()=>expect(recommendationExpiry(new Date('2026-07-13T00:00:00Z'),2).toISOString()).toBe('2026-07-13T02:00:00.000Z'));});
