const HOUR = 3_600_000;
export function recommendationExpiry(createdAt: Date, leadTimeDays: number): Date { if (!Number.isInteger(leadTimeDays) || leadTimeDays < 0) throw new RangeError('leadTimeDays must be a non-negative integer'); if (leadTimeDays <= 2) return new Date(createdAt.getTime()+2*HOUR); if (leadTimeDays <= 7) return new Date(createdAt.getTime()+6*HOUR); if (leadTimeDays <= 30) { const parts = new Intl.DateTimeFormat('en-CA',{timeZone:'Asia/Ho_Chi_Minh',year:'numeric',month:'2-digit',day:'2-digit'}).formatToParts(createdAt); const get=(t:string)=>parts.find(p=>p.type===t)?.value ?? ''; return new Date(`${get('year')}-${get('month')}-${get('day')}T23:59:00+07:00`); } return new Date(createdAt.getTime()+24*HOUR); }
