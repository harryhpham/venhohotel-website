import { GuardrailViolationError } from '@venho/shared';
export interface GuardrailInput { readonly candidateRate: number; readonly floor: number; readonly ceiling: number; readonly commission: number; readonly promoStacking: number; readonly paymentCost: number; readonly minAcceptableNetRate: number; }
export interface GuardrailResult { readonly finalRate: number; readonly effectiveNetRate: number; }
export function enforceGuardrails(input: GuardrailInput): GuardrailResult { for (const [key,value] of Object.entries(input)) if (!Number.isInteger(value) || value < 0) throw new GuardrailViolationError(`${key} must be a non-negative VND integer`, { key, value }); if (input.floor > input.ceiling) throw new GuardrailViolationError('floor exceeds ceiling', { ...input }); const finalRate = Math.min(input.ceiling, Math.max(input.floor, input.candidateRate)); const effectiveNetRate = finalRate-input.commission-input.promoStacking-input.paymentCost; if (effectiveNetRate < input.minAcceptableNetRate) throw new GuardrailViolationError('effective net rate is below minimum', { effectiveNetRate, minimum: input.minAcceptableNetRate }); return { finalRate, effectiveNetRate }; }
