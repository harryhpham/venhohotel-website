import PlaceholderSection from "./PlaceholderSection";

export default function PublishingSection() {
  return (
    <PlaceholderSection
      title="Publishing"
      description="Approval queue và lịch đăng content — Facebook, Instagram, Google Business, Website"
      tools={["Content Approval Queue", "Post Scheduler", "Channel Status"]}
      stage="Stage C"
    />
  );
}
