import PlaceholderSection from "./PlaceholderSection";

export default function SettingsSection() {
  return (
    <PlaceholderSection
      title="Settings"
      description="Cấu hình workspace, API keys, provider settings, automation rules"
      stage="Stage C"
    />
  );
}
