import PlaceholderSection from "./PlaceholderSection";

export default function TasksSection() {
  return (
    <PlaceholderSection
      title="Tasks"
      description="Task tracking và focus scoring — Today's Focus được tính tự động từ domain logic"
      stage="Stage C"
    />
  );
}
