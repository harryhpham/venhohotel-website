import PlaceholderSection from "./PlaceholderSection";

export default function AgentsSection() {
  return (
    <PlaceholderSection
      title="Agents"
      description="Agent control plane — theo dõi và điều phối các AI agents đang chạy"
      tools={["A4 OTA Revenue Agent", "Social Content Agent", "Competitor Analysis Agent"]}
      stage="Stage C"
    />
  );
}
